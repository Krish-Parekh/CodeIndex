[project]
name = "codeindex"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Krish-Parekh", email = "krishparekh303@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
codeindex = "codeindex:main"

[build-system]
requires = ["uv_build>=0.11.9,<0.12.0"]
build-backend = "uv_build"
